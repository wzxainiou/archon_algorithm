# Core dependencies for Jetson Orin Nano benchmarking
ultralytics>=8.0.0
opencv-python>=4.8.0
psutil>=5.9.0
numpy>=1.24.0

# Dual-stream hunting camera dependencies
pyserial>=3.5  # Serial port communication for PTZ control
scipy>=1.10.0  # Spatial distance calculations for tracking

# Optional dependencies for enhanced functionality
# onnxruntime-gpu  # Install separately on Jetson if needed
# tensorrt  # Pre-installed on JetPack
